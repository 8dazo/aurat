{
  "name": "aurat-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && cp browser-view.html dist/ && electron dist/main.js",
    "build": "tsc && cp browser-view.html dist/"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^34.0.0",
    "typescript": "^5.7.0"
  }
}
